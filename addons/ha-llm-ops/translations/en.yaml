configuration:
  log_level:
    name: Log level
    description: Logging verbosity
  buffer_size:
    name: Buffer size
    description: Number of log lines to retain
  problem_dir:
    name: Problem directory
    description: Folder to store problem files
  llm_backend:
    name: LLM backend
    description: Backend used for analysis
  openai_api_key:
    name: OpenAI API key
    description: API key used when OpenAI backend is selected
